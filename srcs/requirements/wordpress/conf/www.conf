[www]

user = www-data
group = www-data

; Listen for NGINX on the Docker network.
listen = 0.0.0.0:9000

pm = dynamic

pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

; Keep container environment variables available.
clear_env = no
